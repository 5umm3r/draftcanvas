# Changelog

## [1.2.21] - 2026-09-04

### 追加

- 画像のブックマーク機能を追加。キャンバスカードと拡大プレビューから切り替えられ、キャンバス上部のフィルタでブックマークした画像だけを表示できる
- 拡大プレビューにアクションバーを追加。ブックマーク、コピー、複製、削除、共有、エクスポートをプレビューから直接操作できる
- 拡大プレビューにキーボードショートカットを追加。⌘B でブックマーク、⌘C でコピー、⌘D で複製、⌘E でエクスポート、Delete で削除。削除後は隣の画像へ自動で移動する
- ブックマーク中の画像を削除から保護。通常表示ではごみ箱ボタンと Delete キーが無効になり、複数選択の一括削除でもブックマーク中の画像はスキップされる。削除したい場合はブックマークを解除するか、ブックマークのみ表示に切り替えてから削除する
- 複数選択中もブックマーク済みの画像にバッジを表示。一括削除で残る画像がひと目で分かる
- プロジェクト削除の確認にブックマーク中の画像の件数を表示

### 変更

- プロンプトパネルから並列実行数メニューを削除。生成条件の設定項目を枚数のみに絞り、操作をシンプルにした

## [1.2.20] - 2026-08-04

### 変更

- iCloud 同期の既定を省容量モードに変更。原本画像は表示するときにダウンロードする方式になり、初回同期の転送量と完了までの時間が大きく減る
- iCloud 同期のステータス表示を残り件数から進捗表示に変更。転送の完了と新しいファイルの追加が同時に起きても数字が逆行しなくなった
- ローカルキャッシュ上限の初期値を 2 GB から 5 GB に変更

### 修正

- 設定画面でローカルキャッシュ上限を変更しても表示に反映されなかった問題を修正
- iCloud 同期のオン・オフ、省容量モード、言語の選択、ローカルデータ削除ボタンの表示が操作直後に更新されないことがある問題を修正
- iCloud にサインイン・サインアウトしても設定画面とサイドバーの表示が追従しなかった問題を修正
- 起動のたびにローカルキャッシュを過剰に削除し、同じ画像を毎回ダウンロードし直していた問題を修正。未ダウンロードのファイルを容量に数えていたことが原因
- アップロード完了直後に同期状態が一時的に戻ることで、ステータスが同期完了と同期中を往復する問題を修正

### 改善

- ローカルキャッシュの削減時に上限の 80% まで削るようにし、起動のたびに削除と再ダウンロードを繰り返す状態を解消
- iCloud のダウンロード要求を一度出したら 30 秒は再送しないようにし、同期処理の混雑を軽減

## [1.2.19] - 2026-07-30

### 追加

- キャンバス画像のプレビュー（カードのダブルクリック / スペースキー）にズーム表示を追加。トラックパッドのピンチ、2 本指ダブルタップ（スマートマグニファイ）、慣性パン、スクロールバーに対応し、最大 800% まで任意倍率で細部を確認できる
- プレビューのコントロールバーに「フィット」「100%」切替、倍率インジケータ、拡大・縮小ボタンを追加。キーボードショートカットは ⌘+ 拡大 / ⌘- 縮小 / ⌘0 フィット / ⌘1 100%
- プレビューにピクセル表示トグルを追加。拡大時に nearest neighbor 描画へ切り替わり、生成画像のアーティファクトをピクセル境界ごと確認できる
- プレビューで前後のアイテムに送ると倍率はフィットに戻る

### 変更

- プレビューで画像の上をクリックしても閉じなくなった（ズーム操作のドラッグと競合するため）。閉じるのは画像外の余白クリック・Esc・スペースキー・閉じるボタン

## [1.2.18] - 2026-07-15

### 追加

- プロンプトパネルに出力スタイル切替（写真/イラスト・アイコン/ロゴ）を追加。デフォルトの「写真/イラスト」では SVG/HTML/CSS/canvas 代替や内部ベクター合成後のラスタライズを明示的に禁止する文言を埋め込み、Codex が SVG→PNG 変換経路を選ぶことによるのっぺり画像事故を防ぐ。「アイコン/ロゴ」選択時はこの禁止条項を外し、ベクター的表現を許容する
- 高解像度化プレビューにズーム表示切替（フィット/100%）を追加し、シート初期サイズを拡大（700×520 → 1100×750）。100% 表示ではスクロールでパンでき、拡大結果の細部をピクセル等倍で確認できる

## [1.2.17] - 2026-07-14

### 修正

- 高解像度化ボタンにCodexコスト消費を示すボルトバッジが表示されたままだった問題を修正（ローカルReal-ESRGAN方式への変更に伴い削除）

## [1.2.16] - 2026-07-14

### 変更

- 高解像度化機能を Codex CLI 経由の AI 再生成方式から、同梱の Real-ESRGAN（`realesrgan-ncnn-vulkan`）によるローカル 4 倍アップスケールに変更。オフラインで数秒〜数十秒で完了し、生成AI特有の意図しない構図・内容変化がなくなった。プレビュー画面で「汎用」「イラスト」モデルを切り替えて再実行できる

## [1.2.15] - 2026-07-14

### 修正

- キャンバスからアイテムを削除しても関連ファイル（マスク `masks/<id>_mask.png` / `_composite.png` / `_preview.png` / `_strokes.json` / `_sketch.png`、添付 `attachments/<id>_*`、切り抜きパラメータ `attachments/<id>_crop.json`）がディスク上に残る問題を修正（`performDelete()` が本体画像とサムネイルしか消していなかった。`ProjectStore.deleteAllFiles(for:)` に統合し、原本画像メモリキャッシュの evict と参照カウント辞書のクリアも同時に実施）
- プロジェクト削除時にサムネイル・マスク・添付ファイルが残る問題を修正（`deleteProject()` が本体画像のみ削除する実装だった。上記統合クリーンアップを各アイテムに適用）

### 追加

- 設定 → メンテナンスに「不要ファイルを削除…」ボタンを追加。過去のバグや異常終了で残った orphan ファイルを一括掃除する
  - 対象: `items/`, `masks/`, `attachments/`, `items/.thumbs[.nosync]/` 配下で、メタデータ `projects.json` の `items[]` に UUID prefix が存在しないファイル
  - iCloud 同期有効時は iCloud コンテナに加えて `~/Library/Application Support/Draft Canvas/` 側の旧ローカルコピーも同時に掃除
  - `~/.codex/generated_images/<threadID>/` に蓄積される Codex 生成キャッシュも掃除（threadID 形式に一致し、mtime が 60 秒以上前のディレクトリのみ削除して生成中のセッションを保護）

## [1.2.14] - 2026-07-13

### 改善

- バリエーション生成の枚数選択を 2/4/6 の固定選択から 1〜12 の任意指定（Stepper）へ変更

### 削除

- プロンプトエンハンス機能（PromptPanel の ✨ ボタン、`PromptEnhancer` / `DraftCanvasViewModel+PromptEnhance`、関連ローカライズ・SPEC 記述）を削除
- 英語正規化機能（`translateToEnglish` トグル、`PromptLanguageNormalizer`、`GenerationRequest.translateToEnglish` / `normalizedPrompt`、`PromptFactory` / `PromptVariator` の英語出力分岐、`promptLanguageMode` 移行処理、関連ローカライズ・SPEC 記述）を削除

## [1.2.13] - 2026-07-13

### 修正

- 画像生成ファイルが `call_*.png` として保存された場合に、DraftCanvas 上の生成中プレースホルダーが完了へ進まず残り続ける問題を修正（`turn/completed` を待つ通常経路に加え、`~/.codex/generated_images/<threadID>/` の生成済み PNG を監視して完了扱いにするフォールバックを追加）

## [1.2.12] - 2026-07-13

### 修正

- Codex CLI 0.144 系で画像生成が成功しているにもかかわらず、DraftCanvas 上では失敗として表示される問題を修正（`image_generation_call.result` が返らず `~/.codex/generated_images/<threadID>/` に PNG が保存される経路に対応）
- Codex が返す `gpt-5.4` / `gpt-5.5` / `gpt-5.6` 系モデルを画像生成候補として表示するよう修正（生成結果の取得側で新しい返却形式に対応し、モデル名による除外を撤廃）

## [1.2.11] - 2026-07-13

### 修正

- Codex の GPT-5.6 系モデルが画像生成結果を返さず生成失敗になる問題を回避（画像生成に未対応のモデルをモデル選択から除外し、既存選択も対応モデルへ正規化）
- 画像生成結果が返らないモデルを選択していた場合のエラー文言を、未対応モデルの可能性が分かる内容に改善

## [1.2.10] - 2026-07-06

### 修正

- 送信端末で画像生成後、iCloud 同期ステータスが「同期中」のまま完了表示に戻らない問題を修正（`NSMetadataQuery` の属性変化通知が発火しない/遅延するケースがあり、アップロード完了が反映されなかった。同期中継続中は 3 秒周期で再評価するポーリングを追加し、ローカル書込直後にも即時再評価をトリガ）
- 省容量モード有効時、`ICloudSyncMonitor` の `autoPullPolicy` が `start()` 呼び出し後に設定されていたため、初回集計が `.eager` 扱いになり未 DL 原本が pending に混入する初期化順序バグを修正

## [1.2.9] - 2026-07-04

### 修正

- 省容量モード有効時、受信側端末の iCloud 同期ステータスが永遠に「同期中」のまま完了しない問題を修正（オンデマンド DL に委ねた未ダウンロード原本を pending 件数から除外。転送中・アップロード未完了のアイテムは従来どおり同期中として集計）

## [1.2.8] - 2026-07-03

### 追加

- iCloud ステータスにオフライン検知・転送エラー表示を追加
- iCloud 同期オフ・未サインイン時の専用ステータス表示を追加

### 修正

- iCloud 同期ステータスが常に「0 KB・同期完了」と表示され、同期中表示・転送エラー検出・自動ダウンロードが機能しない問題を修正（`NSMetadataQuery` のプレーン URL スコープは Spotlight 非索引の `~/Library/Mobile Documents` で常に 0 件になるため、Ubiquitous スコープ + コンテナパス限定の predicate に変更）
- モデル一覧未取得時にモデル自動選択でクラッシュする問題を修正
- 生成サーバー無応答時にリクエストが無限に待機する問題を修正（60 秒タイムアウト）
- 素材分解・背景除去・高解像度化のキャンセル後に旧タスクの結果が反映される問題を修正
- SVG ベクタ化の内部エラーでアプリ全体がクラッシュする問題を修正
- 保存失敗（ディスクフル等）が通知されず編集が黙って消える問題を修正（トースト通知で可視化）
- projects.json 破損時に全プロジェクトが消えたように見える問題を修正（破損ファイルを退避し復旧可能に）
- iCloud 競合バージョンを削除前にローカルへ退避するよう修正

### パフォーマンス

- 操作毎の全件保存をデバウンス + 直列化し UI ブロックを解消
- 生成完了時の画像書込・サムネイル生成をバックグラウンド化
- 比較スライダー・添付サムネイルの毎フレーム再デコードを解消
- 画像キャッシュをメモリ専用化しメインスレッドの同期デコードを排除
- バッチ ZIP エクスポートを 3 並列化
- 起動時の外部ツールパス解決をキャッシュ化し数秒のブロックを排除

## [1.2.7] - 2026-06-30

### 修正

- iCloud 同期が完了しない問題を修正（`NSMetadataQuery` の監視範囲を iCloud Drive 全体から自アプリの Ubiquity コンテナ配下に限定。他アプリのアップロード待機ファイルが pending 件数に混入し、同期状態が `.syncing` のまま固着する原因を解消）

## [1.2.6] - 2026-06-29

### 追加

- 省容量モード（オプション）— iCloud 画像をオンデマンドダウンロードし、ローカルキャッシュ容量上限を設定可能（1/2/5/10 GB・無制限）
- ローカルキャッシュの LRU 退避（容量上限超過時に最終アクセスが古い原本を `evictUbiquitousItem` で解放）
- 未ダウンロード原本に DL 進行オーバーレイ表示
- 最近開いたプロジェクトの起動時先読み（省容量モード時）

### 変更

- iCloud 同期インジケータをパーセンテージから indeterminate スピナーに変更（pending 100 件超で固着する不具合を解消）
- フォアグラウンド復帰時に NSMetadataQuery を再評価して状態固着を回避
- 画像読込パス（Crop / Outpaint / Upscale / Vectorize / MaterialExtract / Export / ItemActions / 各 ViewModel）を ICloudImageLoader 経由の async I/O に統一

### 修正

- iCloud 同期プログレスバーが pending 件数の分母固定により早期に頭打ちする問題

## [1.2.5] - 2026-06-22

### 変更

- 再編集モードでも生成中に次の生成を開始できるよう並行投入を許可（マスク・添付画像は最後の参照解放時のみ削除）

## [1.2.4] - 2026-06-17

### 追加

- マイテンプレートをカテゴリ先頭・初期選択に変更
- テンプレート行全体クリックで適用、ホバー時に行ハイライト

### 変更

- テンプレートの編集・削除をメニューから並列ボタンに変更（削除は赤色）
- キャンバスカードの削除ボタンを赤シンボル化

### 修正

- 参照画像サムネ等でプロンプトパネルが拡張された際にキャンバス最下段の画像がパネル裏に隠れる問題を修正

## [1.2.3] - 2026-06-16

### 修正

- iCloud Drive 同期時に他端末のファイルが自動ダウンロードされない問題を修正

## [1.2.2] - 2026-06-16

### 修正

- リリースビルドで iCloud Drive 同期が動作しない問題を修正（再署名時に iCloud 環境設定が欠落）

## [1.2.1] - 2026-06-16

### 修正

- iCloud Drive 同期がリリースビルド起動時に有効にならない問題を修正

## [1.2.0] - 2026-06-16

### 追加

- iCloud Drive 同期機能（プロジェクト・画像・設定・プロンプト履歴を複数 Mac 間で同期）

## [1.1.1] - 2026-06-09

### 修正

- 再編集モード中にプロンプトパネルが勝手にコンパクトモードに切り替わる問題を修正

## [1.1.0] - 2026-06-08

### 変更

- オープンソース化（MIT License）— ソースコード全公開・アプリ無料配布
- ライセンス検証・トライアル機能を撤去（全機能を制限なく利用可能に）

## [1.0.8] - 2026-06-04

### 追加

- 生成中プレースホルダーのアニメーションスタイル選択機能
- エディタシートのリサイズ対応

### 改善

- キャンバスのズーム・選択切替にスムーズアニメーション追加
- 新ジョブ追加時にキャンバスを先頭ジョブへ自動スクロール

### パフォーマンス

- 動作の改善

## [1.0.7] - 2026-06-02

### 追加

- アウトペイント拡張機能を追加
- 生成メタデータ（モデル名・推論強度・生成時間）の記録に対応

### 改善

- テンプレート一覧を横スクロールから4列縦グリッドに変更
- 完了・失敗ジョブの即時削除とキャンセル対応を統一

## [1.0.6] - 2026-06-01

### 改善

- テンプレートパネルをサムネイル横スクロール形式に刷新
- 履歴パネル カードUI刷新・適用後フォーカス改善

### 修正

- バッチ生成キャンセル後にプレースホルダーが復活する問題を修正
- リトライ中に preparedRequest が早期削除されるバグを修正
- バッチ PNG 最大圧縮時のハングと ZIP ファイル名重複を修正
- バッチ生成の表示順が不安定になる問題を修正
- テンプレート/履歴パネルとプロンプトパネルの状態連動を修正

## [1.0.5] - 2026-05-30

### 追加

- プロンプトテンプレート機能（カテゴリ別40件）
- プロンプト履歴機能

### 修正

- PNG 最大圧縮時に pngquant が動作しない問題を修正（動的リンク依存を解消）

### 改善

- 最大圧縮時の oxipng 最適化レベルを強化（-o2 → -o4、メタデータ完全除去）

## [1.0.4] - 2026-05-27

### 修正

- トリミング後のエクスポートサイズが +1px になるバグを修正

### 改善

- PNG エクスポートの動作を改善

## [1.0.3] - 2026-05-25

### 追加

- WebP 形式でのエクスポートに対応

### 改善

- アプリ終了時に処理中のタスクを保護するよう改善
